export enum BoardPrefixes {
  COLUMN = "container-",
  ITEM = "card-",
}

export enum TableNames {
  COLUMN = "board_column",
  TASK = "task",
  BOARD = "boards",
}

export enum ButtonStyle {
  PRIMARY = "primary",
  DASHED = "dashed",
}
